# LLM Provider: "openai" (default) or "anthropic"
LLM_PROVIDER=openai

# OpenAI (Volcano Engine Ark, OpenAI compatible)
OPENAI_THINK_MODEL=deepseek-v4-flash
OPENAI_THINK_API_KEY=
OPENAI_THINK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
OPENAI_QUICK_MODEL=deepseek-v4-flash
OPENAI_QUICK_API_KEY=
OPENAI_QUICK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# Anthropic (used when LLM_PROVIDER=anthropic)
ANTHROPIC_THINK_MODEL=deepseek-v4-flash
ANTHROPIC_THINK_API_KEY=
ANTHROPIC_THINK_BASE_URL=https://api.anthropic.com
ANTHROPIC_QUICK_MODEL=deepseek-v4-flash
ANTHROPIC_QUICK_API_KEY=
ANTHROPIC_QUICK_BASE_URL=https://api.anthropic.com
ANTHROPIC_THINKING=true
ANTHROPIC_MAX_OUTPUT_TOKENS=8192

# Alibaba DashScope embedding (OpenAI compatible, for RAG knowledge base)
# Used when EMBEDDING_PROVIDER=openai
OPENAI_EMBEDDING_MODEL=text-embedding-v4
OPENAI_EMBEDDING_API_KEY=
OPENAI_EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Ollama local embedding (OpenAI compatible endpoint, v0.1.24+)
# Used when EMBEDDING_PROVIDER=ollama
OLLAMA_EMBEDDING_MODEL=nomic-embed-text
OLLAMA_BASE_URL=http://localhost:11434

# Embedding provider: "openai" (default) | "ollama"
EMBEDDING_PROVIDER=openai

# Note: the vector index dimension is probed from the embedding provider at
# startup, so no dimension configuration is needed.

# Redis (Vector Search, requires Redis Stack with RediSearch module)
REDIS_URL=redis://localhost:6379
REDIS_INDEX_NAME=idx:biz
REDIS_KEY_PREFIX=biz:

# MCP (Log tool SSE)
MCP_URL=http://localhost:3000/sse

# File upload directory (for knowledge base documents)
FILE_DIR=./data/docs

# Prometheus
PROMETHEUS_BASE_URL=http://127.0.0.1:9090
